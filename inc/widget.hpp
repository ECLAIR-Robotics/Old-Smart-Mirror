#include <string>

using std::string;

class Widget{
    public:
        // Dimension and locations for each widget
        int position_x;
        int position_y;
        int height;
        int width;

        string name;

        // Default constructor
        Widget();
};